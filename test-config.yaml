scan:
  static_reachability: true
  tools: ["trivy", "python_reachability", "dynamic_reachability"]
  runtime:
    enabled: true
    timeout: 120
    coverage_wait: 30
    container_port: 3000

risk:
  exposure: private
  data_sensitivity: low

policy:
  block_if: []